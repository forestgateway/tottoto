<Window x:Class="todochart.Views.TaskPropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:todochart.ViewModels"
        Title="プロパティ"
        Width="480" Height="540"
        MinWidth="360" MinHeight="400"
        Background="{StaticResource WindowBgBrush}"
        Foreground="{StaticResource WindowFgBrush}"
        FontFamily="Meiryo UI, Segoe UI, sans-serif" FontSize="13"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource SubTextBrush}"/>
            <Setter Property="Padding"    Value="0,2"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background"   Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground"   Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="BorderBrush"  Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding"      Value="4,3"/>
            <Setter Property="CaretBrush"   Value="{StaticResource AccentBrush}"/>
        </Style>

        <!-- CheckBox: ダーク□インジケーター -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource SubTextBrush}"/>
            <Setter Property="Margin"     Value="0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="box"
                                    Width="14" Height="14"
                                    Background="{StaticResource PanelBgBrush}"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    BorderThickness="1" CornerRadius="2"
                                    VerticalAlignment="Center">
                                <TextBlock x:Name="check" Text="✔"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontSize="9" FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Margin="6,0,0,0" VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="box"   Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="Background"  Value="#2200C8FF"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DatePicker 外枠 -->
        <Style TargetType="DatePicker">
            <Setter Property="Background"  Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground"  Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        </Style>

        <!-- DatePickerTextBox: 内部テキスト入力の白背景を解消 -->
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Background"      Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground"      Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="CaretBrush"      Value="{StaticResource AccentBrush}"/>
            <Setter Property="SelectionBrush"  Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="2,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid Margin="{TemplateBinding Padding}">
                                <ContentControl x:Name="PART_Watermark"
                                                Focusable="False" IsHitTestVisible="False"
                                                Opacity="0.4"
                                                Foreground="{StaticResource SubTextBrush}"
                                                VerticalAlignment="Center"
                                                Visibility="Collapsed"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center"
                                              Background="Transparent"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox: ドロップダウン含めてダーク化 -->
        <Style TargetType="ComboBox">
            <Setter Property="Background"  Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground"  Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding"     Value="6,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="root">
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1" CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      Margin="{TemplateBinding Padding}"
                                                      VerticalAlignment="Center"
                                                      IsHitTestVisible="False"/>
                                    <Path Grid.Column="1"
                                          Data="M0,0 L4,4 L8,0"
                                          Stroke="{StaticResource SubTextBrush}"
                                          StrokeThickness="1.5"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsHitTestVisible="False"/>
                                </Grid>
                            </Border>
                            <!-- クリック領域用の透明 ToggleButton -->
                            <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press" Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{StaticResource PanelBgBrush}"
                                        BorderBrush="{StaticResource AccentBrush}"
                                        BorderThickness="1"
                                        MinWidth="{TemplateBinding ActualWidth}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="root" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem: ドロップダウン各行 -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="Padding"    Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2200C8FF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#3300C8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- GroupBox: ダーク枠 + ヘッダー -->
        <Style TargetType="GroupBox">
            <Setter Property="Foreground"  Value="{StaticResource SubTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background"  Value="Transparent"/>
            <Setter Property="Padding"     Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.RowSpan="2"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1" CornerRadius="4"
                                    Background="{TemplateBinding Background}"
                                    Margin="0,8,0,0"/>
                            <Border Grid.Row="0"
                                    Background="{StaticResource WindowBgBrush}"
                                    HorizontalAlignment="Left"
                                    Margin="10,0,0,0" Padding="4,0">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"
                                                  TextElement.FontSize="11"/>
                            </Border>
                            <ContentPresenter Grid.Row="1"
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button: ToolBarButtonStyle ベース -->
        <Style TargetType="Button" BasedOn="{StaticResource ToolBarButtonStyle}">
            <Setter Property="Padding"    Value="16,6"/>
            <Setter Property="Foreground" Value="{StaticResource WindowFgBrush}"/>
        </Style>

        <!-- TabControl: コンテンツ背景をダーク化 -->
        <Style TargetType="TabControl">
            <Setter Property="Background"  Value="{StaticResource WindowBgBrush}"/>
            <Setter Property="Foreground"  Value="{StaticResource WindowFgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0"
                                      IsItemsHost="True"
                                      Background="Transparent"
                                      Panel.ZIndex="1"/>
                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="0,4,4,4">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  Margin="4"
                                                  KeyboardNavigation.TabNavigation="Local"
                                                  KeyboardNavigation.DirectionalNavigation="Contained"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabItem: 選択・ホバー色 -->
        <Style TargetType="TabItem">
            <Setter Property="Background"  Value="{StaticResource PanelBgBrush}"/>
            <Setter Property="Foreground"  Value="{StaticResource SubTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding"     Value="12,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1,1,1,0"
                                CornerRadius="4,4,0,0"
                                Margin="0,0,2,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background"  Value="{StaticResource HeaderBgBrush}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground"                  Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#2200C8FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- 名前 -->
            <RowDefinition Height="*"/>     <!-- タブ -->
            <RowDefinition Height="Auto"/>  <!-- ボタン -->
        </Grid.RowDefinitions>

        <!-- ── 名前 ──────────────────────────────────────── -->
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <Label Content="名前"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="13"/>
        </StackPanel>

        <!-- ── タブ ──────────────────────────────────────── -->
        <TabControl x:Name="MainTabControl" Grid.Row="1">

            <!-- 日程タブ（ToDo のみ） -->
            <TabItem x:Name="ScheduleTab" Header="日程">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <!-- 完了チェック + WAIT + 進捗率 -->
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,12"
                                    VerticalAlignment="Center">
                            <CheckBox IsChecked="{Binding Completed}"
                                      Content="完了済み"
                                      VerticalAlignment="Center"/>
                            <CheckBox IsChecked="{Binding IsWait}"
                                      Content="WAIT"
                                      Margin="12,0,0,0"
                                      VerticalAlignment="Center"/>
                            <TextBlock Text="進捗率:"
                                       Margin="16,0,4,0"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource SubTextBrush}"/>
                            <ComboBox ItemsSource="{Binding ProgressOptions}"
                                      SelectedItem="{Binding Progress}"
                                      Width="76"
                                      VerticalAlignment="Center"
                                      ItemStringFormat="{}{0}%"
                                      IsEnabled="{Binding IsNotFolder}"/>
                        </StackPanel>

                        <!-- 開始日 -->
                        <GroupBox Header="開始日" Foreground="{StaticResource SubTextBrush}"
                                  BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,8">
                            <StackPanel Margin="6">
                                <CheckBox IsChecked="{Binding HasBeginDate}"
                                          Content="開始日を設定する"/>
                                <DatePicker SelectedDate="{Binding BeginDate}"
                                            IsEnabled="{Binding BeginDateEnabled}"
                                            Margin="0,4,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- 終了日 -->
                        <GroupBox Header="終了日" Foreground="{StaticResource SubTextBrush}"
                                  BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,8">
                            <StackPanel Margin="6">
                                <CheckBox IsChecked="{Binding HasEndDate}"
                                          Content="終了日を設定する"/>
                                <DatePicker SelectedDate="{Binding EndDate}"
                                            IsEnabled="{Binding EndDateEnabled}"
                                            Margin="0,4,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- 日数カウント方法 -->
                        <GroupBox Header="日数カウント" Foreground="{StaticResource SubTextBrush}"
                                  BorderBrush="{StaticResource BorderBrush}" Margin="0,0,0,8">
                            <StackPanel Margin="6">
                                <ComboBox SelectedIndex="{Binding DateCountLevel}">
                                    <ComboBoxItem Content="全日カウント"/>
                                    <ComboBoxItem Content="土日を除外"/>
                                    <ComboBoxItem Content="休日すべて除外"/>
                                </ComboBox>
                            </StackPanel>
                        </GroupBox>

                        <!-- 期間表示 -->
                        <TextBlock Text="{Binding DaysLabel}"
                                   Foreground="{StaticResource AccentBrush}"
                                   Margin="0,4"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- メモタブ -->
            <TabItem x:Name="MemoTab" Header="メモ">
                <TextBox Text="{Binding Memo, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Margin="4"/>
            </TabItem>

            <!-- リンクタブ -->
            <TabItem x:Name="LinkTab" Header="リンク">
                <StackPanel Margin="8">
                    <Label Content="ファイル / フォルダ / URL"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding Link, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="1" Content="📂" Margin="4,0,0,0"
                                Click="OnBrowseFileClick" ToolTip="ファイルを参照"/>
                        <Button Grid.Column="2" Content="🌐" Margin="4,0,0,0"
                                Click="OnOpenLinkClick"   ToolTip="リンクを開く"/>
                    </Grid>
                    <Button Content="デスクトップアプリで開く先頭文字を付与" Margin="0,8,0,0"
                            HorizontalAlignment="Left"
                            Click="OnApplyDesktopAppPrefixClick"
                            IsEnabled="{Binding CanApplyDesktopAppPrefix}"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- ── OK / キャンセル ─────────────────────────────── -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="OK"
                    IsDefault="True"
                    MinWidth="80"
                    Click="OnOkClick"/>
            <Button Content="キャンセル"
                    IsCancel="True"
                    MinWidth="80"
                    Margin="8,0,0,0"
                    Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
