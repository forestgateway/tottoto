<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- カラーは各テーマの辞書 (Themes/Colors/*.xaml) で定義されます -->
    <!-- BaseTheme はブラシのみを定義し、ブラシの Color はテーマ辞書の Color リソースを DynamicResource で参照します -->
    <SolidColorBrush x:Key="AccentBrush"       Color="{DynamicResource AccentColor}"/>
    <SolidColorBrush x:Key="PanelBgBrush"      Color="{DynamicResource PanelBg}"/>
    <SolidColorBrush x:Key="HeaderBgBrush"     Color="{DynamicResource HeaderBg}"/>
    <SolidColorBrush x:Key="RowSelectedBrush"  Color="{DynamicResource RowSelected}"/>
    <SolidColorBrush x:Key="WindowBgBrush"     Color="{DynamicResource WindowBg}"/>
    <SolidColorBrush x:Key="WindowFgBrush"     Color="{DynamicResource WindowFg}"/>
    <SolidColorBrush x:Key="SubTextBrush"      Color="{DynamicResource SubText}"/>
    <SolidColorBrush x:Key="BorderBrush"       Color="{DynamicResource Border}"/>
    <SolidColorBrush x:Key="ChartBgBrush"      Color="{DynamicResource ChartBg}"/>
    <SolidColorBrush x:Key="RowEvenBrush"      Color="{DynamicResource RowEven}"/>
    <SolidColorBrush x:Key="RowOddBrush"       Color="{DynamicResource RowOdd}"/>
    <SolidColorBrush x:Key="GridLineBrush"     Color="{DynamicResource GridLine}"/>

    <!-- ── 共通定数 ──────────────────────────────────── -->
    <sys:Double x:Key="RowHeight">24</sys:Double>
    <sys:Double x:Key="CellWidth">22</sys:Double>
    <sys:Double x:Key="HeaderHeight">36</sys:Double>

    <!-- ガントチャートの進行表示（進捗線）関連のデフォルト設定
         - GanttProgressBarHeight: 進行バーの高さ（ピクセル）。行高さに応じて調整されます。
         - GanttProgressCornerRadius: 角丸半径（ピクセル）。
         - GanttProgressHorizontalGap: セル間の水平ギャップ（通常は 0 にして隙間を無くす）
         これらは各テーマファイルで上書き可能です -->
    <sys:Double x:Key="GanttProgressBarHeight">18</sys:Double>
    <sys:Double x:Key="GanttProgressCornerRadius">0</sys:Double>
    <sys:Double x:Key="GanttProgressHorizontalGap">0</sys:Double>

    <!-- ── ToolBar ボタン ─────────────────────────────── -->
    <Style x:Key="ToolBarButtonStyle" TargetType="Button">
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="BorderBrush"  Value="Transparent"/>
        <Setter Property="Foreground"   Value="{DynamicResource SubTextBrush}"/>
        <Setter Property="Padding"      Value="8,0"/>
        <Setter Property="FontSize"     Value="12"/>
        <Setter Property="FontFamily"   Value="Meiryo UI, Segoe UI, sans-serif"/>
        <Setter Property="Cursor"       Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="5"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background"   Value="#1A0070C0"/>
                            <Setter TargetName="bd" Property="BorderBrush"  Value="#440070C0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background"   Value="#330070C0"/>
                            <Setter TargetName="bd" Property="BorderBrush"  Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#FFBBBBBB"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- メニュー・コンテキストメニューの共通スタイル -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource WindowFgBrush}"/>
    </Style>

    <!-- ContextMenu: 白枠を排除するためカスタムTemplateで完全上書き -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource WindowFgBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <StackPanel IsItemsHost="True"
                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MenuItem: サブメニューのPopup白枠も排除するため完全上書き -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource WindowFgBrush}"/>
        <Setter Property="Padding" Value="6,3"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIconColumn"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumn"/>
                                <ColumnDefinition Width="17"/>
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="Icon" Grid.Column="0"
                                              ContentSource="Icon"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="Center"
                                              Width="16" Height="16" Margin="4,0,6,0"/>
                            <ContentPresenter Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="Center"/>
                            <TextBlock x:Name="IGT" Grid.Column="2"
                                       Text="{TemplateBinding InputGestureText}"
                                       Foreground="{DynamicResource SubTextBrush}"
                                       Margin="14,0,0,0" VerticalAlignment="Center"/>
                            <!-- ポップアップ: トップレベルは Bottom、サブメニューは Right (Trigger で切替) -->
                            <Popup x:Name="PART_Popup"
                                   Grid.Column="0" Grid.ColumnSpan="4"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="{DynamicResource PanelBgBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1">
                                    <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- サブメニュー項目は Popup を右に表示 -->
                        <Trigger Property="Role" Value="SubmenuHeader">
                            <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource SubTextBrush}"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Separator: メニュー内区切り線もテーマ色で描画 -->
    <Style TargetType="Separator">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="4,2,4,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{DynamicResource BorderBrush}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── 更新ボタン（強調表示用）────────────────────────── -->
    <Style x:Key="UpdateButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolBarButtonStyle}">
        <!-- やや濃い青で白字 -->
        <Setter Property="Background" Value="#FF1E4DB6"/>
        <Setter Property="Foreground" Value="#FFFFFFFF"/>
        <Setter Property="BorderBrush" Value="#FF17428E"/>
    </Style>

    <!-- ── ToggleButton（展開／折りたたみ用） ─────────── -->
    <Style x:Key="ExpandToggleStyle" TargetType="Button">
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="BorderBrush"  Value="Transparent"/>
        <Setter Property="Foreground"   Value="#FF888888"/>
        <Setter Property="FontSize"     Value="9"/>
        <Setter Property="Padding"      Value="1"/>
        <Setter Property="Cursor"       Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="Transparent" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ListBox / ListView の行スタイル ──────────── -->
    <Style x:Key="TaskRowContainerStyle" TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Padding"         Value="0"/>
        <Setter Property="Height"          Value="{DynamicResource RowHeight}"/>
        <Setter Property="Background"      Value="Transparent"/>
        <Setter Property="BorderBrush"     Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ToolBar トグルボタン ────────────────────────── -->
    <Style x:Key="ToolBarToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="BorderBrush"  Value="Transparent"/>
        <Setter Property="Foreground"   Value="{DynamicResource SubTextBrush}"/>
        <Setter Property="Padding"      Value="8,4"/>
        <Setter Property="FontSize"     Value="13"/>
        <Setter Property="FontFamily"   Value="Meiryo UI, Segoe UI, sans-serif"/>
        <Setter Property="Cursor"       Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="5"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="bd" Property="Background"  Value="{DynamicResource RowSelectedBrush}"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background"   Value="#1A0070C0"/>
                            <Setter TargetName="bd" Property="BorderBrush"  Value="#440070C0"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background"   Value="#330070C0"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#FFBBBBBB"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ScrollViewer カスタムテンプレート（薄いスクロールバー） ── -->
    <Style x:Key="ThinScrollStyle" TargetType="ScrollViewer">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="1" Grid.Row="0"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                   Width="8"/>
                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                   Grid.Column="0" Grid.Row="1"
                                   Orientation="Horizontal"
                                   Value="{TemplateBinding HorizontalOffset}"
                                   Maximum="{TemplateBinding ScrollableWidth}"
                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                   Height="8"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── ColumnHeader ─────────────────────────────── -->
    <Style x:Key="ColumnHeaderStyle" TargetType="TextBlock">
        <Setter Property="Background"          Value="Transparent"/>
        <Setter Property="Foreground"          Value="{DynamicResource SubTextBrush}"/>
        <Setter Property="FontSize"            Value="12"/>
        <Setter Property="FontWeight"          Value="SemiBold"/>
        <Setter Property="VerticalAlignment"   Value="Center"/>
        <Setter Property="Padding"             Value="6,0"/>
        <Setter Property="TextTrimming"        Value="CharacterEllipsis"/>
    </Style>

</ResourceDictionary>
